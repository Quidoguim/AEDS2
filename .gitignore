# macOS
.DS_Store
**/.DS_Store

# Claude Code (configuração local, não compartilhada)
.claude/settings.local.json

# Java (compilado localmente, sem build tool / metadados do VSCode)
*.class
bin/
.classpath
.project
.settings/
